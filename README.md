# FastLoad

FastLoad is an experimental Forge 1.12.2 coremod that reduces repeated mod-discovery work between launches.

The implementation lives on the `feature/fastload-v0.1` branch while the first build is being verified.
